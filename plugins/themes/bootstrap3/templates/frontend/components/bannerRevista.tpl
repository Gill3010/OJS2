{if $currentContext}
    <!-- Banner visible solo en la revista -->
    <div class="revista-banner">
        <img src="{$baseUrl}/public/banners/bannerRevista1.png" alt="Banner de la Revista" class="img-responsive">
    </div>
{/if}
